# 后端服务端口。前端默认会请求 http://localhost:3002。
PORT=3002

# 可选：接入 OpenAI 兼容模型。
# 如果不配置 API Key，Agent 会使用本地规则回复，方便先跑通完整链路。
# 接智谱 GLM 时，可以把 base URL、model 和 key 改成智谱控制台提供的值。
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=glm-5
